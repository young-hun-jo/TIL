- window function이나 연산을 사용해서 만든 새로운 변수는 where절에 변수로 사용못하기 때문에 서브쿼리 이용해 테이블 하나로 묶어줘서 사용하자!
-SQL 사용자 정의 함수 안에 SELECT, 서브쿼리 문 가능해!!
-Array는 같은 원소만 가능!
-SQL limit에서 해당 조건을 만족하는 데이터가 없으면 자동으로 NULL 반환
-LIKE는 대소문자 구분 안하고 모두 포함
-대소문자 구분시키려면 정규표현식(REGEXP) 사용하기!함!
-ORDER BY할때 파생변수 만들어서 정렬 가능함!
-MySQL에 XOR연산자도 있음!!
-공백 포함하는 행 조건-> 변수명 LIKE ‘% %’ 로 가능!
-MySQL 이스케이프 문자 = \(백슬레시)
